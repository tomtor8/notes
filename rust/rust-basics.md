# Setup on Arch

- install `rustup` via pacman
- then run `rustup default stable` which installs cargo, rustc and everything and sets up the environment
- install rust-analyzer by running `rustup component add rust-analyzer`
